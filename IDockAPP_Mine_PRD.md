# iDock AI耳机APP —「我的」模块 产品需求文档 (PRD)

> 版本：v5.1\
> 更新日期：2026-07-27\
> 原型文件：[IDockAPP\_Mine.html](IDockAPP_Mine.html)

***

## 一、产品概述

### 1.1 产品定位与目标用户

iDock 是一款 AI 耳机配套 APP，「我的」模块是整个 APP 的个人中心，承载用户的身份管理、用量查询、功能配置、会员订阅、设备管理等个人化能力。它的核心用户群体是 iDock AI 耳机持有者——包括需要管理设备与偏好的日常用户、需要查看用量和管理订阅的付费会员、以及需要借助权益对比做出购买决策的潜在付费用户。

用户打开「我的」的主要动机只有两类：**查看状态**（我的额度还剩多少、我的会员什么时候到期），以及**修改配置**（换一个音色、改一下翻译语言）。因此整个模块的信息架构遵循「信息优先、按需深入、反馈即时」的原则——高频查看的信息直接在首页卡片中呈现，低频修改的操作收敛到二级页面，所有设置项修改后即时生效并给出明确反馈。

### 1.2 页面架构总览

```
「我的」首页
├── 身份区（点击 → 个人信息）                 ← 一级入口
├── 用量仪表盘（点击 → 会员方案）              ← 一级入口
├── 功能导航
│   ├── 我的设备                            ← 一级入口
│   │   ├── 通用设置
│   │   ├── 手势设置（弹窗）
│   │   ├── 声音设置 → AI声音
│   │   ├── 查找设备（弹窗）
│   │   └── 固件更新（弹窗）
│   ├── AI录音                              ← 一级入口
│   │   ├── AI生成模式
│   │   ├── 默认模板总结
│   │   └── 默认转写语言
│   ├── AI翻译                              ← 一级入口
│   │   ├── 默认翻译语言
│   │   └── 专业术语偏好
│   ├── AI声音                              ← 一级入口
│   │   ├── 系统声音选择(默认男声/默认女声)
│   │   └── 自定义声音(上传音频创建)
│   └── 记忆管理                             ← 一级入口
├── 管理入口
│   ├── 通用设置                             ← 一级入口
│   │   ├── 界面语言
│   │   ├── 主题模式
│   │   ├── 字体大小
│   │   ├── 个人词库
│   │   └── 声纹管理
│   ├── 专属客服                             ← 一级入口
│   │   ├── 帮助与反馈
│   │   └── 人工客服
│   └── 关于                                ← 一级入口
├── 切换账号（按钮）
└── 退出登录（按钮）
```

### 1.3 页面清单

**一级页面** — 整个模块的根页面

| 页面标题   | 说明              |
| ------ | --------------- |
| 「我的」首页 | 个人中心主页面，所有功能的入口 |

**二级页面** — 从「我的」首页直接进入

| 页面标题 | 入口来源  | 说明                  |
| ---- | ----- | ------------------- |
| 个人信息 | 身份区卡片 | 头像/昵称/ID/手机号管理      |
| 会员方案 | 用量仪表盘 | 档位切换+周期选择+权益对比+支付   |
| 设备管理 | 功能导航  | 多设备切换+状态监控+设备设置     |
| AI录音 | 功能导航  | 无痕模式+AI生成模式+默认模板总结  |
| AI翻译 | 功能导航  | 翻译语言+术语偏好+人名保留      |
| AI声音 | 功能导航  | 系统声音选择+自定义声音创建+语速调节 |
| 记忆管理 | 功能导航  | AI记忆库开关             |
| 通用设置 | 管理入口  | 外观/通知/存储/个性功能       |
| 专属客服 | 管理入口  | FAQ搜索+分类浏览+人工客服     |
| 关于   | 管理入口  | 版本号/官网/官媒/法律文档/版权   |

**三级页面** — 从二级页面进入

| 页面标题    | 所属上级页面 | 说明             |
| ------- | ------ | -------------- |
| 订阅管理    | 会员方案   | 当前方案+自动续费+订单记录 |
| AI生成模式  | AI录音   | 手动生成/自动生成配置    |
| 默认模板总结  | AI录音   | 5种模板+自定义模板选择   |
| 默认转写语言  | AI录音   | 7种语言选择         |
| 默认翻译语言  | AI翻译   | 7种语言选择         |
| 专业术语偏好  | AI翻译   | 6个领域选择         |
| 自定义声音   | AI声音   | 起名+选图标+上传音频→生成 |
| 声纹管理    | 通用设置   | 声纹录入+列表+删除     |
| 个人词库    | 通用设置   | 录音词库/翻译词库双标签页  |
| 自定义翻译词库 | 个人词库   | 原文→译文映射对       |
| 界面语言    | 通用设置   | 4种语言选择         |
| 主题模式    | 通用设置   | 跟随系统/浅色/深色     |
| 字体大小    | 通用设置   | 5级滑块+预览        |

***

## 二、功能设计

### 2.1 「我的」首页

这是整个个人中心的一级入口页面，核心职责是让用户在最短路径内获取关键状态信息并导航到目标功能。

**功能描述**：个人中心主页面，展示身份信息、用量概况、功能导航入口和管理入口，是用户查看状态和进入各子功能的统一入口。

**功能场景使用**：用户张经理刚开完一场 1 小时的客户会议，打开 APP 想确认今天的转写额度还剩多少——首页不用任何点击就能看到进度条还剩 45%。他顺便注意到 AI 声音显示的是"默认男声"，想换成女声，点击「AI声音」进入二级页面切换。

**字段表格**：

| 字段     | 来源    | 说明                   |
| ------ | ----- | -------------------- |
| 头像     | 个人信息  | 当前用户头像，点击进入个人信息      |
| 昵称     | 个人信息  | 当前用户昵称               |
| ID（脱敏） | 账号系统  | 格式如 8888\*\*\*\*6666 |
| 会员等级   | 会员系统  | 如"标准版""专业版""旗舰版"     |
| 到期时间   | 会员系统  | 当前周期到期日              |
| 转写进度   | 用量系统  | 已用/总量，进度条形式          |
| 功能摘要   | 各功能配置 | 如"已连接""中文（简体）""温柔女声" |

**交互情况**：点击身份区卡片进入个人信息页；点击用量区卡片进入会员方案页；点击功能导航栏各项进入对应二级页面；点击「切换账号」弹出账号列表切换；点击「退出登录」退出当前账号。

**边缘情况**：退出登录为不可逆操作，按钮红色已暗示风险；切换账号不清除当前账号缓存数据；状态摘要须在对应配置变更后即时同步更新。

**限制条件**：无特殊技术限制。

**注意事项**：首页为纯展示+导航页面，不做数据修改操作。功能导航中的状态摘要依赖各子功能的配置回调。

**异常处理**：头像加载失败使用默认占位图；网络异常时用量数据显示"--"。

**原型图**：

> ![「我的」首页](./screenshots/01_profile.png)

**用户流程**：

```
「我的」首页
    │
    ├─ 点击身份区卡片 ──→ 个人信息
    │
    ├─ 点击用量区（右侧会员等级/进度条）──→ 会员方案
    │
    ├─ 功能导航
    │   ├─ 我的设备 ──→ 设备管理
    │   ├─ AI录音   ──→ AI录音页
    │   ├─ AI翻译   ──→ AI翻译页
    │   ├─ AI声音   ──→ AI声音页
    │   └─ 记忆管理 ──→ 记忆管理页
    │
    ├─ 管理入口
    │   ├─ 通用设置 ──→ 通用设置页
    │   ├─ 专属客服 ──→ 专属客服页
    │   └─ 关于     ──→ 关于页
    │
    ├─ 切换账号（按钮）
    └─ 退出登录（按钮）
```

#### 2.1.1 身份区

**功能描述**：以简洁的卡片形式展示头像、昵称和脱敏 ID，头像右下角提供更换入口（点击弹出底部操作菜单：拍照/相册）。整卡可点击进入个人信息编辑页。

**功能场景使用**：用户打开首页即可看到当前身份信息，无需任何点击。想修改头像时点击右下角相机图标弹出菜单选择拍照或相册。

**字段表格**：

| 字段 | 类型  | 说明                       |
| -- | --- | ------------------------ |
| 头像 | 图片  | 圆形裁剪，默认占位图               |
| 昵称 | 字符串 | 最长 20 字符                 |
| ID | 字符串 | 脱敏展示，格式 8888\*\*\*\*6666 |

**交互情况**：首页直接展示，点击头像右下角相机图标弹出底部菜单（拍照/相册），点击整卡进入个人信息编辑。

**边缘情况**：头像加载失败时使用默认占位图替代；高频查看的信息直接可见，低频修改的操作收敛到二级页面。

**限制条件**：昵称最长 20 字符；ID 格式固定不可修改。

**注意事项**：整卡可点击进入个人信息，不需要额外"编辑"按钮以减少视觉噪音。

**异常处理**：头像加载失败使用默认占位图。

**原型图**：同上「我的」首页原型图。

**用户流程**：点击身份区卡片 → 个人信息页。

#### 2.1.2 用量仪表盘

**功能描述**：内嵌在身份区卡片右侧，展示当前会员等级（如"专业版"）、到期时间及实时转写进度条。整张用量卡片可点击进入会员方案页。

**功能场景使用**：用户看到额度快用完时，点击用量区直接进入会员方案页对比升级。

**字段表格**：

| 字段   | 类型  | 说明          |
| ---- | --- | ----------- |
| 会员等级 | 枚举  | 标准版/专业版/旗舰版 |
| 到期时间 | 日期  | 当前周期到期日     |
| 转写进度 | 进度条 | 已用比例，非精确百分比 |

**交互情况**：首页直接查看，点击整张用量卡片进入会员方案页。

**边缘情况**：进度条实时反映当前周期已使用比例，仅供快速感知余量；会员到期时显示"已到期"。

**限制条件**：进度条非精确百分比，仅供快速感知。

**注意事项**：进度条数据从用量系统获取，更新频率为近实时（约 5 分钟延迟）。

**异常处理**：用量数据获取失败时进度条显示"--"。

**原型图**：同上「我的」首页原型图。

**用户流程**：点击用量区卡片 → 会员方案页。

#### 2.1.3 功能导航

**功能描述**：功能入口分为两组。第一组是功能导航——我的设备、AI录音、AI翻译、AI声音、记忆管理，展示当前状态摘要。第二组是管理入口——通用设置、专属客服、关于，纯导航入口。

**功能场景使用**：用户点击任意入口进入对应二级页面。状态摘要帮助在首页即可快速感知各功能当前配置。

**字段表格**：

| 入口   | 状态摘要示例        |
| ---- | ------------- |
| 我的设备 | "已连接" / "未连接" |
| AI录音 | 当前转写语言        |
| AI翻译 | 当前翻译语言        |
| AI声音 | 当前声音名称        |
| 记忆管理 | "已开启" / "已关闭" |

**交互情况**：点击功能导航项进入对应二级页面；管理入口项无状态摘要，仅导航。

**边缘情况**：状态摘要须在配置变更后即时同步更新，确保所见即所得。

**限制条件**：管理入口无状态摘要，纯导航。

**注意事项**：功能导航中的摘要依赖各子功能页面的配置回调通知。

**异常处理**：若某功能配置获取失败，摘要显示"--"。

**原型图**：同上「我的」首页原型图。

**用户流程**：点击任意入口 → 对应二级页面。

#### 2.1.4 底部操作

**功能描述**：提供切换账号和退出登录两个操作入口。

**功能场景使用**：用户有多账号时点击切换账号；要安全退出时点击退出登录红色按钮。

**字段表格**：无独立数据字段。

**交互情况**：点击「切换账号」按钮弹出账号列表切换；点击「退出登录」红色按钮直接注销并返回登录页。

**边缘情况**：退出登录为不可逆操作，按钮颜色已暗示风险；切换账号时不清除当前账号缓存数据。

**限制条件**：切换账号仅在多账号登录场景下可用。

**注意事项**：退出登录后需重新输入密码登录；切换账号不会清除本地缓存数据。

**异常处理**：账号列表加载失败时显示重试按钮。

**原型图**：同上「我的」首页原型图。

**用户流程**：切换账号 → 选择账号；退出登录 → 返回登录页。

***

### 2.2 身份卡区域

**功能描述**：承载用户的身份管理和会员订阅信息，是「我的」首页身份区和用量仪表盘对应的卡片内容区域。包含个人信息管理（头像、昵称、ID、手机号）和会员方案（档位切换、周期选择、权益对比、支付、订阅管理）。

**功能场景使用**：用户点击首页身份区卡片进入个人信息编辑，点击首页用量区进入会员方案对比升级。

**字段表格**：

| 区域   | 字段           | 说明      |
| ---- | ------------ | ------- |
| 个人信息 | 头像/昵称/ID/手机号 | 账号形象管理  |
| 会员订阅 | 档位/周期/价格/权益  | 会员方案与支付 |

**交互情况**：点击首页身份区卡片进入个人信息编辑；点击首页用量区进入会员方案对比升级。

**边缘情况**：身份区卡片和用量仪表盘属于同卡，整卡可点击。个人信息编辑和会员方案为同页内两个区域，无需跳转二级页面。

**限制条件**：同页内切换，个人信息区和会员区独立交互互不影响。

**注意事项**：个人信息修改即时同步首页；会员方案需确认支付后才生效。

**异常处理**：数据加载失败分别显示对应区域的重试按钮。

**原型图**：

> ![「我的」首页](./screenshots/01_profile.png)

**用户流程**：

```
身份卡区域
    │
    ├─ 个人信息 ──→ 编辑头像/昵称/ID/手机号
    │
    └─ 会员订阅 ──→ 选择档位+周期 → 权益对比 → 支付 → 订阅管理
```

#### 2.2.1 个人信息

**功能场景使用**：

- **换头像**：王工参加行业峰会，拍了一张新的商务照，想换掉之前的卡通头像以显得更专业。
- **改昵称**：李老师入职新公司后，想将昵称从旧公司的"XX科技-李"改为"YY教育-李老师"。
- **复制ID**：客服要求提供设备 ID 以排查连接问题，用户点击 ID 行一键复制后粘贴给客服。
- **换绑手机号**：张总换了新手机号，需要将账号绑定到新号码以确保找回密码功能可用。

**字段表格**：

| 字段  | 类型  | 规则            | 说明        |
| --- | --- | ------------- | --------- |
| 头像  | 图片  | 支持拍照/相册上传     | 圆形裁剪显示    |
| 昵称  | 字符串 | 最长 20 字符，不可为空 | 编辑弹窗修改    |
| ID  | 字符串 | 系统分配，不可修改     | 脱敏展示+一键复制 |
| 手机号 | 字符串 | 11 位数字        | 脱敏展示+验证换绑 |

**交互情况**：头像——点击右下角相机图标弹出底部菜单（拍照/相册/预览）；昵称——点击行弹出编辑弹窗；ID——点击行一键复制并轻提示；手机号——点击弹出详情弹窗，支持显示/隐藏和换绑。

**边缘情况**：拍照和相册均需系统权限，权限被拒时轻提示引导前往设置；头像加载失败使用默认占位图；昵称为空或超长阻止保存并轻提示；手机号格式不正确阻止下一步并轻提示；验证码错误轻提示重新输入；完整 ID 为敏感信息，复制后仅保留在剪贴板中。

**限制条件**：昵称最长 20 字符；ID 不可修改；手机号 11 位数字格式。

**注意事项**：更换手机号后原号码不可用于找回密码；ID 复制后退出 APP 系统将自动清除剪贴板；头像上传建议使用 1:1 比例图片。

**异常处理**：权限被拒时轻提示引导前往系统设置；头像加载失败使用默认占位图；昵称为空/超长阻止保存并轻提示；手机号格式不正确阻止下一步并轻提示；验证码错误轻提示重新输入；短信验证码发送失败轻提示重试。

**原型图**：

> ![个人信息](./screenshots/02_personal_info.png)

**用户流程**：

```
个人信息
    │
    ├─ 点击头像右下角相机图标
    │   ├─ 拍照 ──→ 权限弹窗 → 系统相机 → 更新头像 + 轻提示
    │   └─ 相册选取 ──→ 权限弹窗 → 系统相册 → 更新头像 + 轻提示
    │
    ├─ 点击头像主体 → 全屏预览大图
    │
    ├─ 点击「昵称」→ 编辑弹窗 → 保存 → 首页同步
    │
    ├─ 点击「ID」→ 复制完整ID到剪贴板 → 轻提示
    │
    └─ 点击「手机号」→ 详情弹窗
        ├─ 显示/隐藏号码
        └─ 更换手机号 → 4步验证流程
```

##### 2.2.1.1 头像修改

**功能描述**：头像居中放大展示，右下角有相机图标。点击弹出底部操作菜单，支持拍照、相册选取和全屏预览三种方式更换头像。

**功能场景使用**：用户想更换专业商务照时，点击相机图标选择「拍照」拍摄新头像或「相册选取」已有照片。

**字段表格**：

| 字段   | 类型 | 规则           |
| ---- | -- | ------------ |
| 头像图片 | 图片 | 支持拍照/相册，圆形成像 |

**交互情况**：点击右下角相机图标弹出底部菜单选择「拍照」或「相册选取」；点击头像主体全屏预览大图。

**边缘情况**：拍照和相册均需请求系统权限，权限被拒时轻提示引导用户前往系统设置开启。头像加载失败时使用默认占位图替代。

**限制条件**：头像支持拍照和相册两种来源；权限被拒后无法使用对应功能。

**注意事项**：拍照需系统相机权限，相册选取需相册读取权限；建议使用 1:1 比例图片。

**异常处理**：权限被拒时轻提示引导前往系统设置；头像加载失败使用默认占位图。

**原型图**：同上个人信息原型图。

**用户流程**：点击相机图标 → 拍照/相册 → 权限弹窗 → 系统应用 → 更新头像 + 轻提示。

##### 2.2.1.2 昵称编辑

**功能描述**：点击「昵称」行弹出编辑弹窗，输入新昵称后确认即时同步到首页。

**功能场景使用**：用户入职新公司后，点击昵称行把旧昵称改为带新公司标识的昵称。

**字段表格**：

| 字段 | 类型  | 规则                       |
| -- | --- | ------------------------ |
| 昵称 | 字符串 | 最长 20 字符，不可为空，支持中英文及特殊符号 |

**交互情况**：点击「昵称」行 → 弹窗中输入新昵称 → 确认保存 → 首页身份区同步刷新显示。

**边缘情况**：超出 20 字符时输入框自动截断，昵称为空时阻止保存并轻提示。

**限制条件**：昵称最长 20 字符，不可为空，支持中英文及特殊符号。

**注意事项**：修改昵称后首页身份区同步刷新。

**异常处理**：昵称为空或超长阻止保存并轻提示。

**原型图**：同上个人信息原型图。

**用户流程**：点击昵称行 → 编辑弹窗 → 输入新昵称 → 确认 → 首页同步。

##### 2.2.1.3 ID 管理

**功能描述**：脱敏展示 ID（如 `8888****6666`），点击整行一键复制完整 ID 到剪贴板。

**功能场景使用**：客服要求提供设备 ID 排查问题，用户点击 ID 行一键复制后粘贴给客服。

**字段表格**：

| 字段 | 类型  | 规则             |
| -- | --- | -------------- |
| ID | 字符串 | 系统分配，脱敏展示，不可修改 |

**交互情况**：点击 ID 行无需弹窗直接复制，轻提示反馈"已复制完整ID至剪贴板"。

**边缘情况**：完整 ID 为敏感信息，复制后仅保留在剪贴板中，退出 APP 后系统自动清除。

**限制条件**：ID 为系统分配，不可修改，脱敏展示。

**注意事项**：复制完整 ID 后请勿分享给不可信的第三方。

**异常处理**：复制失败时轻提示"复制失败，请重试"。

**原型图**：同上个人信息原型图。

**用户流程**：点击 ID 行 → 一键复制 → 粘贴给客服。

##### 2.2.1.4 手机号管理

**功能描述**：脱敏展示手机号（如 `138****8888`），点击弹出详情弹窗，支持完整号码显示/隐藏切换和更换手机号的多步验证流程。

**功能场景使用**：用户换新手机号后，点击手机号行进入详情弹窗，点击「更换手机号」触发 4 步验证流程换绑。

**字段表格**：

| 字段  | 类型  | 规则          |
| --- | --- | ----------- |
| 手机号 | 字符串 | 11 位数字，脱敏展示 |
| 验证码 | 字符串 | 6 位数字，短信获取  |

**交互情况**：点击弹出详情弹窗 → 可切换显示/隐藏完整号码；点击「更换手机号」→ 4 步验证（验证原号 → 输入新号 → 验证新号 → 确认更换）。

**边缘情况**：手机号格式不正确时阻止进入下一步并轻提示；验证码错误时轻提示重新输入；更换后原号码不可用于找回密码。

**限制条件**：手机号 11 位数字格式；验证码 6 位数字，60 秒内有效；更换流程需原号和新号均可接收短信。

**注意事项**：更换后原号码不可用于找回密码；验证码每天最多发送 5 次。

**异常处理**：手机号格式不正确阻止下一步并轻提示；验证码错误轻提示重新输入；验证码发送失败轻提示"发送失败，请稍后重试"。

**原型图**：同上个人信息原型图。

**用户流程**：点击手机号 → 详情弹窗 → 显示/隐藏号码 / 更换手机号 → 4 步验证。

#### 2.2.2 会员订阅

**功能描述**：会员方案页是付费转化的核心页面，页面名称为「选择您的会员方案」。支持规格选择（Terra/Sol）、周期选择（连续包年/包月/1年/3个月/1个月）、核心权益展示和权益对比弹窗。右上角有「管理」按钮跳转订阅管理。

**功能场景使用**：

- 小刘是 Luna 免费用户，额度快用完，点击用量区进入会员方案页，对比后选择了 Terra 连续包年。
- 赵总是 Terra 会员，点击右上角「管理」进入订阅管理确认续费状态。
- 新用户小陈点击「查看权益对比」弹窗逐行比较实时转写时长和蒜粒积分后做决策。

**字段表格**：

| 字段   | 类型 | 说明                   |
| ---- | -- | -------------------- |
| 规格   | 枚举 | Terra / Sol           |
| 周期   | 枚举 | 连续包年/连续包月/1年/3个月/1个月 |
| 价格   | 数字 | 随规格+周期联动变化           |
| 核心权益 | 列表 | 转写时长/翻译时长/蒜粒积分     |
| 协议勾选 | 布尔 | 默认未勾选                |

**交互情况**：切换规格卡片时周期价格、核心权益联动刷新；左右滑动或点击周期卡片切换；点击「查看权益对比」弹出底部弹窗；弹窗内点击版本卡片用淡金色透明框高亮对应列；勾选协议后点击主按钮触发支付确认弹窗。

**边缘情况**：同档位显示"当前计划"并置灰；协议未勾选时弹出确认弹窗引导勾选；当前计划重复购买时底部按钮置灰。

**限制条件**：仅支持两个付费规格（Terra/Sol）和五个周期选项；权益对比表为静态数据，不包含实时价格和促销信息。

**注意事项**：切换规格不等于购买，需选择周期并确认支付后才生效。协议默认未勾选，每次切换规格或周期后重置。取消续订后当前周期内仍可正常使用，到期后自动降为 Luna，取消不可撤回。

**异常处理**：支付失败时轻提示"支付未完成，请重试"并停留在当前页面；网络异常时价格和权益数据可能为空，显示加载中状态；协议未勾选点击支付弹出确认弹窗引导勾选。

**原型图**：

> ![会员方案](./screenshots/03_membership.png)

**用户流程**：

```
会员方案
    │
    ├─ 点击规格卡片（Terra / Sol）→ 周期价格/核心权益联动刷新
    │
    ├─ 左右箭头滑动/点击周期卡片 ──→ 选中高亮
    │
    ├─ 点击「查看权益对比」──→ 弹出底部弹窗
    │   ├─ 点击 Luna / Terra / Sol 版本卡片 ──→ 淡金色透明框高亮该列
    │   └─ 点击关闭或下拉 → 收起弹窗
    │
    ├─ 勾选协议 + 点击底部按钮
    │   ├─ 协议未勾选 ──→ 确认弹窗 → 自动勾选 → 再次点击
    │   └─ 协议已勾选 ──→ 支付确认弹窗 → 确认 → 轻提示 → 跳转支付
    │
    └─ 点击右上角「管理」──→ 订阅管理
        ├─ 查看当前方案详情
        ├─ 取消续订 → 轻提示
        └─ 查看订单记录列表
```

##### 2.2.2.1 规格选择

**功能描述**：卡片式规格选择区，区域标题为「选择规格」。两个规格卡片——Terra（标准，¥264/月）和 Sol（专业，¥689/月）。Sol 卡片标注「推荐」角标。点击卡片切换规格，选中卡片蓝色边框高亮，下方价格和权益联动刷新。

**功能场景使用**：用户进入会员方案页，点击 Terra 和 Sol 卡片对比各规格价格和权益差异。

**字段表格**：

| 规格    | 名称 | 定位  | 月价     | 标注   |
| ----- | -- | --- | ------ | ---- |
| Luna  | 免费 | 入门  | ¥0     | 无    |
| Terra | 标准 | 主力  | ¥264   | 无    |
| Sol   | 专业 | 旗舰  | ¥689   | 推荐标签 |

> 注：Luna 免费规格在会员方案页不展示选择卡片，仅作为基线出现在权益对比弹窗中。

**交互情况**：点击规格卡片切换，选中蓝色边框+阴影高亮。规格切换后周期卡片价格和核心权益同步刷新。

**边缘情况**：切换规格不等于购买，需选择周期并确认支付后才生效。

**限制条件**：固定三个规格档位，仅 Terra 和 Sol 在购买页中可选。

**注意事项**：Sol 默认标注推荐标签；切换规格不等于购买。

**异常处理**：规格数据加载失败时显示重试按钮。

**原型图**：同上会员方案原型图。

**用户流程**：点击规格卡片 → 所有信息联动刷新。

##### 2.2.2.2 周期选择

**功能描述**：横向滚动卡片 + 左右箭头，提供五个周期选项（连续包年推荐标签+首年特惠、连续包月首月特惠、1年、3个月、1个月），选中周期卡片蓝色边框高亮。首月特惠标记显示在卡片底部。

**功能场景使用**：用户对比连续包年和1年总价差异后选择最优方案。

**定价策略**：

| 规格    | 月原价  | 连续包月（首月八折） | 连续包年（首年五折） | 1年原价   | 3个月   | 1个月  |
| ----- | ---- | ---------- | ---------- | ------ | ----- | ---- |
| Terra | ¥264 | ¥211       | ¥1,584     | ¥3,168 | ¥792  | ¥264 |
| Sol   | ¥689 | ¥551       | ¥4,134     | ¥8,268 | ¥2,067 | ¥689 |

**字段表格**：

| 周期   | 续费方式       | 优惠标记  |
| ---- | ---------- | ----- |
| 连续包年 | 自动续费（推荐标签） | 首年特惠  |
| 连续包月 | 自动续费       | 首月特惠  |
| 1年   | 一次性        | 无     |
| 3个月  | 一次性        | 无     |
| 1个月  | 一次性        | 无     |

**交互情况**：点击周期卡片或左右箭头切换，选中蓝色边框高亮。连续包年/包月卡片底部显示优惠标记（首年特惠/首月特惠），原价用删除线展示。连续包年/包月自动续费，非连续套餐到期自动停止。

**边缘情况**：选中显示的价格为「总价/周期时长」计算后的月均/年均价格。

**限制条件**：固定五个周期选项，不可自定义。

**注意事项**：连续包年/包月自动续费，非连续套餐到期自动停止。

**异常处理**：价格数据加载失败时显示"--"。

**原型图**：同上会员方案原型图。

**用户流程**：点击周期卡片 → 选中高亮 → 价格刷新。

##### 2.2.2.3 核心权益

**功能描述**：三列宫格展示当前选中规格的核心权益（实时转写时长、翻译时长、蒜粒积分），右侧有「查看权益对比」链接点击后弹出权益对比弹窗。

**权益明细**：

| 权益     | Luna   | Terra     | Sol      |
| ------ | ------ | --------- | -------- |
| 实时转写时长 | 300min/月 | 1800min/月 | 6000min/月 |
| 翻译时长   | 300min/月 | 1800min/月 | 6000min/月 |
| 蒜粒积分   | 3000积分  | 6600积分    | 9900积分    |

**交互情况**：切换规格时宫格数据联动刷新；点击「查看权益对比」弹出底部弹窗。

**边缘情况**：核心权益仅展示 3 项最重要的权益，完整对比表包含 5 大类 14+ 行。

**限制条件**：核心权益仅展示 3 项，更多权益需查看完整对比表。

**注意事项**：点击「查看权益对比」弹出弹窗而非页面内滚动。

**异常处理**：权益数据加载失败时显示"--"。

**原型图**：同上会员方案原型图。

**用户流程**：切换规格 → 核心权益刷新 / 点击「查看权益对比」→ 弹出弹窗。

##### 2.2.2.4 权益对比弹窗

**功能描述**：底部弹出式弹窗，标题为「完整权益对比」。弹窗上方放置 Luna/Terra/Sol 三张版本卡片，点击任意卡片用淡金色透明框高亮对应列（`border:2px solid rgba(251,191,36,0.4); background:rgba(251,191,36,0.06)`）。下方为五大类逐行对比表（AI录音 / AI翻译 / AI总结 / Agent中心 / 其他），共 14+ 行。弹窗可通过关闭按钮或点击遮罩关闭。

**功能场景使用**：新用户点击「查看权益对比」弹出弹窗，点击不同版本卡片对比权益差异，某权益仅更高档位有时低档位列显示"--"。

**字段表格**：

| 权益分类    | 权益行数         |
| ------- | ------------ |
| AI录音    | 实时转写、说话人识别、声纹录入、热词库 |
| AI翻译    | 翻译时长、翻译语种、支持语种    |
| AI总结    | 纪要模板、图文纪要、自定义模板    |
| Agent中心 | 蒜粒积分、画图、Agent接入、PPT、天气、新闻 |
| 其他      | 设备绑定、云端存储、个人词库、客服支持  |

**交互情况**：点击版本卡片切换高亮列；所有数值统一为灰色（`text-gray-600`），低档位无权益显示"--"（`text-gray-300`），有权益显示绿色"✓"。弹窗上端有拖拽指示条和关闭按钮。

**边缘情况**：权益对比表为静态数据，不包含实时价格和促销信息。所有权益以购买页展示为准。

**限制条件**：权益对比表为静态数据，不含实时价格。

**注意事项**：低档位无某权益时显示"--"；所有权益以购买页展示为准；弹窗打开后默认高亮 Terra 列。

**异常处理**：对比表加载失败时显示占位文本"数据加载中"。

**原型图**：同上会员方案原型图。

**用户流程**：打开弹窗 → 点击版本卡片 → 淡金色框高亮列联动切换 → 关闭弹窗。

##### 2.2.2.5 支付流程

**功能描述**：底部固定栏，包含「变更为该计划」/「升级为XX版」主按钮和支付协议勾选（《电子付费协议》《自动续费条款》）。

**功能场景使用**：用户选定规格和周期后，勾选协议点击主按钮触发支付确认弹窗。

**字段表格**：

| 元素   | 类型  | 说明               |
| ---- | --- | ---------------- |
| 主按钮  | 按钮  | 文案动态变化           |
| 支付协议 | 勾选框 | 默认未勾选，切换档位或周期后重置 |

**交互情况**：勾选支付协议 → 点击主按钮 → 支付确认弹窗 → 确认后跳转支付。未勾选协议时弹出确认弹窗引导勾选。

**边缘情况**：主按钮文案动态调整——同档位显示"当前计划"并置灰，高于当前档位显示"升级为XX版"。当前计划重复购买时底部按钮置灰显示"当前计划"。

**限制条件**：仅支持三个档位五个周期，不可自定义。

**注意事项**：协议默认未勾选，每次切换档位或周期后重置。

**异常处理**：支付失败时轻提示"支付未完成，请重试"并停留在当前页；协议未勾选点击支付弹出确认弹窗引导勾选。

**原型图**：同上会员方案原型图。

**用户流程**：勾选协议 → 点击主按钮 → 支付确认弹窗 → 跳转支付。

##### 2.2.2.6 订阅管理

**功能描述**：从会员方案页右上角「管理」进入，查看当前方案详情，支持自动续费开关、取消续订及历史订单列表查询。

**功能场景使用**：用户想确认下个月是否会自动扣费，进入订阅管理查看续费状态和到期日。

**字段表格**：

| 字段   | 说明          |
| ---- | ----------- |
| 方案名  | 当前订阅方案名称    |
| 状态   | 生效中/已到期/已取消 |
| 到期日  | 当前周期到期日期    |
| 续费金额 | 下一周期续费价格    |
| 自动续费 | 开/关         |
| 订单记录 | 历史订单列表      |

**交互情况**：右上角「管理」→ 查看当前方案卡片 → 可关闭自动续费开关或点击「取消续订」→ 确认弹窗后取消。下滑查看历史订单列表。

**边缘情况**：取消续订后当前周期内仍可正常使用，到期后自动降为免费版，取消操作不可撤回。会员到期时用量归零或显示基础版额度，标注"已到期"。

**限制条件**：取消续订不可撤回；订单记录最多展示近 12 个月。

**注意事项**：取消续订后当前周期仍可正常使用，到期自动降为免费版；恢复订阅需重新支付。

**异常处理**：订单记录加载失败时显示重试按钮；取消续订操作失败时轻提示"操作失败，请稍后重试"。

**原型图**：

> ![订阅管理](./screenshots/04_order_manage.png)

**用户流程**：右上角「管理」→ 查看方案详情 / 取消续订 / 查看订单记录。

***

### 2.3 我的设备

**功能描述**：管理已绑定的 iDock 设备，支持多设备切换、重命名、状态监控（电量 / 存储 / 信号），以及手势设置、声音设置、查找设备、固件更新、解除绑定等设备相关操作。

**功能场景使用**：

- 陈工有两台设备（办公用 Vox Clip，出差用充电宝 D399），进办公室点击设备标签切换到 Vox Clip。
- 小刘买了新 Vox Clip 后将设备名改为"小刘的工牌-会议室专用"避免混淆。
- 张经理把耳机随手放在会议室，点击「查找设备」让耳机发出提示音定位。
- APP 提示"发现新版本"，用户点击固件更新查看日志并升级。
- 用户出二手设备，点击「解除绑定」清除个人数据。

**字段表格**：

| 字段    | 类型   | 说明         |
| ----- | ---- | ---------- |
| 设备名称  | 字符串  | 可编辑修改      |
| 设备型号  | 字符串  | 只读         |
| 序列号   | 字符串  | 唯一标识，只读    |
| 连接状态  | 枚举   | 已连接/离线     |
| 耳机电量  | 百分比  | 左耳/右耳分别显示  |
| 语音盒电量 | 百分比  | 充电盒电量      |
| 4G 信号 | 信号强度 | 语音盒信号      |
| 存储使用量 | 分钟数  | 已用/总共，带进度条 |

**交互情况**：顶部设备标签切换当前设备；右上角「+」添加新设备；点击笔图标重命名；设备设置列表跳转对应功能；危险操作需二次确认弹窗。

**边缘情况**：同一账号最多绑定 5 台设备；离线设备仅显示基本信息，状态监控数据不可用；设备未连接时副标题显示"未连接"；序列号不可修改用于售后和绑定校验；恢复出厂设置清除所有数据不可恢复。

**限制条件**：同一账号最多绑定 5 台设备；序列号只读不可修改；存储容量因设备型号而异。

**注意事项**：解除绑定后设备可被其他账号绑定，个人数据在解绑时自动清除；恢复出厂设置恢复为出厂状态，所有配置和数据不可恢复；设备名称修改仅影响本 APP 显示，不改变设备硬件标识。

**异常处理**：设备离线时状态监控区显示灰色"--"；存储使用量为估算值，误差 ±5%；设备未连接时轻提示"设备未连接，请检查蓝牙"。

**原型图**：

> ![我的设备](./screenshots/05_my_devices.png)

**用户流程**：

```
我的设备
    │
    ├─ 点击设备标签切换设备 → 设备详情/状态同步切换
    │
    ├─ 点击笔图标 → 重命名弹窗 → 确认
    │
    ├─ 设备设置
    │   ├─ 通用设置 ──→ 通用设置页
    │   ├─ 手势设置 → 弹窗（暂为提示窗占位）
    │   ├─ 声音设置 ──→ AI声音页
    │   ├─ 查找设备 → 弹窗（暂为提示窗占位）
    │   └─ 固件更新 → 弹窗（暂为提示窗占位）
    │
    ├─ 解除绑定 → 二次确认弹窗 → 不可逆
    └─ 恢复出厂设置 → 二次确认弹窗 → 清除所有数据
```

#### 2.4.1 设备切换

**功能描述**：顶部设备标签切换设备，每个标签含在线/离线状态指示灯（绿点=在线，灰点=离线）。右上角「+」按钮添加新设备。预设三台设备：Vox Clip（已连接）、智能充电站（已连接）、充电宝D399（离线）。

**功能场景使用**：用户有多台设备时点击顶部标签快速切换管理。

**字段表格**：

| 字段    | 说明          |
| ----- | ----------- |
| 设备名称  | 预设或用户自定义名称  |
| 状态指示灯 | 绿点=在线，灰点=离线 |

**交互情况**：点击顶部标签切换当前管理设备；点击「+」按钮引导添加新设备。

**边缘情况**：同一账号最多可绑定 5 台设备。离线设备仅显示基本信息，设备未连接时副标题显示"未连接"。

**限制条件**：同一账号最多绑定 5 台设备。

**注意事项**：离线设备仅显示基本信息，状态监控数据不可用。

**异常处理**：设备列表加载失败时显示重试按钮；设备切换失败时保持上一次选中设备。

**原型图**：同上我的设备原型图。

**用户流程**：点击设备标签 → 切换设备。

#### 2.3.2 设备详情

**功能描述**：展示设备名称（可点击笔图标弹窗修改）、型号、序列号、连接状态文本和设备矢量示意图。

**功能场景使用**：用户点击笔图标重命名设备，查看序列号用于售后。

**字段表格**：

| 字段   | 类型  | 规则      |
| ---- | --- | ------- |
| 设备名称 | 字符串 | 可编辑修改   |
| 型号   | 字符串 | 只读      |
| 序列号  | 字符串 | 唯一标识，只读 |
| 连接状态 | 枚举  | 已连接/离线  |

**交互情况**：点击设备名称旁的笔图标弹出重命名弹窗，输入新名称后确认。

**边缘情况**：序列号为设备唯一标识，不可修改，用于售后和绑定校验。

**限制条件**：型号和序列号为只读；设备名称可编辑修改。

**注意事项**：序列号不可修改，用于售后和绑定校验。

**异常处理**：设备详情加载失败时显示重试按钮。

**原型图**：同上我的设备原型图。

**用户流程**：点击笔图标 → 重命名弹窗 → 确认。

#### 2.3.3 状态监控

**功能描述**：双列宫格展示设备实时状态——耳机（左耳/右耳分别显示电量）、语音盒（电量 + 4G 信号强度）、存储（已用/总共分钟数，带进度条）。

**功能场景使用**：用户进入设备管理页直接查看电量和存储，无需额外点击。

**字段表格**：

| 监控项 | 字段        | 说明       |
| --- | --------- | -------- |
| 耳机  | 左耳电量、右耳电量 | 分别显示百分比  |
| 语音盒 | 电量、4G信号强度 | 百分比+信号格数 |
| 存储  | 已用分钟/总共分钟 | 进度条      |

**交互情况**：进入设备管理页直接查看，数据自动刷新。

**边缘情况**：离线设备或未连接组件显示为灰色"--"状态。存储使用量为估算值，误差 ±5% 内。

**限制条件**：数据自动刷新，频率取决于设备连接状态；离线设备无实时数据。

**注意事项**：存储使用量为估算值，误差 ±5% 内。

**异常处理**：离线设备或未连接组件显示灰色"--"；数据刷新失败保持上一次有效数据。

**原型图**：同上我的设备原型图。

**用户流程**：进入设备管理 → 直接查看状态。

#### 2.3.4 设备设置列表

**功能描述**：设备设置入口列表，包含通用设置、手势设置（弹窗）、声音设置（跳转AI声音页）、查找设备（弹窗）、固件更新（弹窗，可显示红色提示）。

**功能场景使用**：用户点击各设置项进入对应功能。有固件新版本时该项显示红色提示文字。

**字段表格**：

| 设置项  | 跳转方式             |
| ---- | ---------------- |
| 通用设置 | 跳转通用设置页          |
| 手势设置 | 弹窗（当前占位）         |
| 声音设置 | 跳转AI声音页          |
| 查找设备 | 弹窗（当前占位）         |
| 固件更新 | 弹窗（当前占位，有新版红色提示） |

**交互情况**：点击各设置项进入对应页面或弹窗。

**边缘情况**：手势设置、查找设备、固件更新当前暂为弹窗占位，后续版本迭代为完整功能页面。

**限制条件**：手势设置、查找设备、固件更新当前为占位弹窗。

**注意事项**：有固件新版本时该项显示红色提示文字。

**异常处理**：占位弹窗显示"功能开发中，敬请期待"。

**原型图**：同上我的设备原型图。

**用户流程**：点击设置项 → 对应页面/弹窗。

#### 2.4.5 危险操作

**功能描述**：提供解除绑定和恢复出厂设置两个危险操作。解除绑定为红色按钮，恢复出厂设置为灰色按钮，均需二次确认弹窗后才执行不可逆操作。

**功能场景使用**：用户出二手设备时点击解除绑定清除个人数据；设备异常时点击恢复出厂设置。

**字段表格**：无独立数据字段。

**交互情况**：解除绑定——点击红色按钮 → 二次确认弹窗 → 确认后解绑并清除数据；恢复出厂设置——点击灰色按钮 → 二次确认弹窗 → 确认后清除设备所有数据。

**边缘情况**：解除绑定后设备可被其他账号绑定，个人数据自动清除。恢复出厂设置不可恢复。

**限制条件**：两个操作均不可逆，需二次确认。

**注意事项**：解除绑定后设备可被其他账号绑定；恢复出厂设置清除设备所有数据。

**异常处理**：操作失败时轻提示"操作失败，请稍后重试"并保持当前状态。

**原型图**：同上我的设备原型图。

**用户流程**：点击按钮 → 二次确认 → 执行不可逆操作。

***

### 2.4 AI录音

**功能描述**：配置 AI 录音的行为偏好。支持无痕模式（录音不保存，仅实时转写）、AI 生成模式（手动或自动生成纪要，可配置触发时长与生成上限）、默认模板总结（6 种纪要模板可选）、默认转写语言等设置。

**功能场景使用**：

- 王律师在敏感商业会议中开启无痕模式——录音不保存仅实时转写，会后自动清除不留痕迹。
- 李经理将触发时长设为 10 分钟——站会跳过正式会议自动生成；将上限设为 2 小时防止午休忘关录音浪费算力。
- 赵博士将默认模板从"通用会议纪要"切换到"学术访谈笔记"，访谈录音整理出来直接就是结构化文档。

**字段表格**：

| 字段     | 类型 | 说明                      |
| ------ | -- | ----------------------- |
| 无痕模式   | 布尔 | 开关，默认关闭                 |
| AI生成模式 | 枚举 | 手动生成/自动生成               |
| 触发时长   | 枚举 | 不限制/1分钟/10分钟/30分钟       |
| 时长上限   | 枚举 | 不限制/1小时/2小时/4小时/6小时/8小时 |
| 默认模板   | 枚举 | 6 种模板可选                 |
| 转写语言   | 枚举 | 7 种语言                   |

**交互情况**：无痕模式 开关 即时生效；点击 AI生成模式进入生成模式配置页；点击默认模板总结进入模板选择页；点击转写语言进入语言选择页。

**边缘情况**：无痕模式下转写文本也不会保存；手动模式需手动点击生成；自动模式超上限时静默切换手动模式并轻提示；转写语言与翻译语言独立配置。

**限制条件**：触发时长档位固定为 4 档（不限制/1分钟/10分钟/30分钟）；时长上限固定为 6 档（不限制/1\~8小时）；转写语言 7 种可选；自定义模板仅旗舰版可用。

**注意事项**：无痕模式下所有新录音均不保存音频文件，转写文本页面关闭即消失。转写语言与翻译语言独立配置，互不影响。模板仅影响纪要生成格式，不影响录音和转写内容。切换到新模板后新录音使用新模板，已生成纪要格式不变。

**异常处理**：录音超时时长上限时自动切换为手动模式并轻提示，不弹窗打扰；触发时长和上限为"非严格截断"，当前录音段结束后生效；模板详情弹窗加载失败时显示默认文本。

**原型图**：

> ![AI录音](./screenshots/06_ai_record.png)

**用户流程**：

```
AI录音
    │
    ├─ 无痕模式 开关 → 即时生效
    │
    ├─ AI生成模式 ──→ AI生成模式页
    │   ├─ 选择「手动生成」
    │   └─ 选择「自动生成」→ 展开配置项
    │       ├─ 触发时长选择 → 选项弹窗
    │       └─ 长度上限选择 → 选项弹窗
    │
    └─ 默认模板总结 ──→ 默认模板总结页
        ├─ 点击模板卡片 → 选中高亮 + 详情弹窗
        └─ 自定义模板（旗舰版专属）
```

#### 2.4.1 无痕模式

**功能描述**：开关，开启后录音不保存，仅实时转写。副标题："保护隐私安全，录音不保存，仅实时转写"。

**功能场景使用**：会议涉及敏感商业信息时，用户提前开启无痕模式确保不留下录音痕迹。

**字段表格**：

| 字段   | 类型 | 默认值   |
| ---- | -- | ----- |
| 无痕模式 | 布尔 | false |

**交互情况**：进入 AI录音页，点击开关开关即时生效。开启后所有新录音均不保存音频文件。

**边缘情况**：无痕模式下录音结束后转写文本同样不会保存，页面关闭即消失。

**限制条件**：仅影响新录音，已保存的录音不受影响。

**注意事项**：开启无痕模式后所有新录音均不保存音频文件；转写文本同样不保存。

**异常处理**：无特殊异常情况。

**原型图**：同上 AI录音原型图。

**用户流程**：点击开关→即时生效。

#### 2.5.2 AI生成

**功能描述**：AI录音页上的导航入口，展示 AI生成模式（显示当前模式摘要如"自动生成 · 不限制时长"）和默认模板总结（显示当前模板名如"通用会议纪要"），点击进入对应页面。

**功能场景使用**：用户在 AI录音页直接查看当前生成配置摘要，点击进入二级配置页修改。

**字段表格**：

| 入口字段   | 显示示例         |
| ------ | ------------ |
| AI生成模式 | 自动生成 · 不限制时长 |
| 默认模板总结 | 通用会议纪要       |

**交互情况**：在 AI录音页直接查看当前摘要，点击任意项进入对应配置页。

**边缘情况**：摘要文本随配置修改即时更新。

**限制条件**：仅展示当前生效配置摘要，不可直接修改。

**注意事项**：摘要文本随 AI生成模式和默认模板总结的配置修改即时更新。

**异常处理**：配置获取失败时摘要显示"--"。

**原型图**：同上 AI录音原型图。

**用户流程**：点击入口 → 对应配置页。

#### 2.4.3 AI生成模式

**功能描述**：选择手动生成（自己决定何时生成）或自动生成（录音结束自动触发）。自动生成可配置触发时长（不限制/1分钟/10分钟/30分钟）和时长上限（不限制/1小时/2小时/4小时/6小时/8小时），通过底部弹出滚轮选择器进行配置。

**功能场景使用**：用户选择「自动生成」后展开配置，点击触发时长/时长上限调出底部滚轮选择器，选择后点击「确定」生效。

**字段表格**：

| 字段   | 类型 | 可选值                     |
| ---- | -- | ----------------------- |
| 生成模式 | 枚举 | 手动生成/自动生成               |
| 触发时长 | 枚举 | 不限制/1分钟/10分钟/30分钟       |
| 时长上限 | 枚举 | 不限制/1小时/2小时/4小时/6小时/8小时 |

**交互情况**：进入 AI生成模式页，选择「手动生成」或「自动生成」。自动生成时展开配置项，点击触发时长/时长上限调出底部滚轮选择器，确定生效、取消放弃。

**边缘情况**：手动模式录音结束后需手动点击「生成纪要」；自动模式下达到触发时长阈值才生成；超上限时静默切换手动模式并轻提示；触发时长和上限非严格截断，当前录音段结束后生效。

**限制条件**：触发时长固定 4 档；时长上限固定 6 档；手动模式仅自己决定。

**注意事项**：自动模式超上限时静默切换，不弹窗打扰；触发时长和上限为"非严格截断"。

**异常处理**：配置保存失败时轻提示"保存失败，请重试"。

**原型图**：

> ![AI生成模式](./screenshots/07_ai_gen_mode.png)

**用户流程**：选择手动/自动 → 自动时配置触发时长和上限 → 滚轮选择器 → 确定。

##### 2.4.4.1 触发时长档位设计

| 档位    | 排除意图           | 适用场景             |
| ----- | -------------- | ---------------- |
| 不限制   | 所有录音均生成        | 不想操心，全量生成        |
| 1 分钟  | 排除误触、测试录音、碎片语音 | 少于 1 分钟无总结价值     |
| 10 分钟 | 排除简短闲聊、快速同步    | 10 分钟以上说明有实质性讨论  |
| 30 分钟 | 排除非正式沟通        | 30 分钟是"标准会议"心理锚点 |

##### 2.4.3.2 时长上限档位设计

| 档位   | 止损场景            |
| ---- | --------------- |
| 不限制  | 由用户自行管理         |
| 1 小时 | 标准会议，默认值        |
| 2 小时 | 长会议、研讨会         |
| 4 小时 | 半天工作会议、培训       |
| 6 小时 | 上午场 + 午休延伸      |
| 8 小时 | 完整工作日（忘记关录音的保底） |

#### 2.4.4 默认模板总结

**功能描述**：页面标题为「默认模板总结」。顶部横向滑动分类标签栏（推荐 / 自定义 / 会议 / 面试 / 电话 / 教育）用于筛选模板。推荐区以 2 列网格展示 6 种纪要模板卡片（图文纪要、可视化报告、会议纪要、日常记录、面试报告、学习笔记）。自定义区包含「新建模板」入口按钮和已创建的自定义模板。选中模板蓝色边框高亮并带蓝色勾选标记。点击卡片右上角小图标弹出底部弹窗预览模板输出示例。底部固定「使用该模板发起听记」主按钮。

**功能场景使用**：用户进入页面浏览模板列表，点击选中模板标记为当前使用。点击卡片右上角图标弹出预览弹窗查看输出格式。底部按钮确认使用选中的模板发起听记。

**字段表格**：

| 模板名称   | 适用场景      | 权限      |
| ------ | --------- | ------- |
| 图文纪要   | 图文融合，快速回顾  | VIP      |
| 可视化报告  | HTML 可视化报告 | VIP      |
| 会议纪要   | 智能总结会议内容   | 全部用户    |
| 日常记录   | 分段总结摘要     | 全部用户    |
| 面试报告   | 企业招聘结构化报告  | 全部用户    |
| 学习笔记   | 精准捕捉课堂精华   | 全部用户    |
| 全天候记录  | 办公室环境下持续记录 | 企业模板    |
| 新建模板   | AI 生成自定义模板 | 全部用户可创建 |

> 注：企业模板仅 Sol 用户可用，VIP 模板需 Terra 或 Sol 方可使用。自定义模板创建流程为：输入提示词 → AI 生成 → 预览修改 → 确认使用。

**交互情况**：点击分类标签切换显示区；点击模板卡片选中（蓝色边框+右上角蓝色勾选标记），再次点击取消选中；点击卡片右上角图标弹出预览弹窗；弹窗中可查看模板说明和输出示例，底部有"使用该模板发起听记"按钮。VIP/企业模板权限不足时弹出 Toast 提示升级。

**边缘情况**：模板仅影响纪要生成格式和结构，不影响录音和转写内容。切换模板后新录音使用新模板，已生成纪要格式不变。页面返回后再进入保持上次选中状态。

**限制条件**：8 种模板（6 推荐 + 2 自定义）。VIP 模板需 Terra 或 Sol，企业模板仅 Sol。

**注意事项**：模板仅影响纪要格式，不影响录音和转写内容；切换后新录音使用新模板；VIP/企业模板权限不足时弹出升级提示不会自动跳转。

**异常处理**：模板列表加载失败时显示重试按钮。

**原型图**：

> ![默认模板总结](./screenshots/08_summary_templates.png)

**用户流程**：浏览模板 → 点击选中 → 高亮标记 / 点击右上角 → 预览弹窗 → 确认使用 / 点击底部按钮 → 发起听记。

***

### 2.5 AI翻译

**功能描述**：配置 AI 翻译的行为偏好。支持默认翻译语言选择（7 种语言）、专业术语偏好设置（6 大领域：通用 / 医疗 / 法律 / 金融 / 科技 / 学术）、人名 / 专名保留开关等设置，确保翻译结果准确且符合场景需求。

**功能场景使用**：

- 陈总监每周和日本团队开视频会，将默认翻译语言设为日语。
- 王医生将专业术语偏好设为"医疗"，使"动脉粥样硬化"准确译为"atherosclerosis"。
- 张总开启"人名/专名保留"，"Apple"不会被翻成"苹果"。

**字段表格**：

| 字段      | 类型 | 说明                |
| ------- | -- | ----------------- |
| 默认翻译语言  | 枚举 | 7 种语言可选           |
| 专业术语偏好  | 枚举 | 通用/医疗/法律/金融/科技/学术 |
| 人名/专名保留 | 布尔 | 开关，默认关闭           |

**交互情况**：点击默认翻译语言进入语言选择页；点击专业术语偏好进入领域选择页；人名/专名保留 开关即时生效。

**边缘情况**：翻译语言与转写语言独立配置；源语言默认跟随转写语言设置；术语偏好为单选项；人名保留依赖 命名实体识别，准确率约 95%。

**限制条件**：翻译语言支持 7 种；术语偏好为单选项（6 个领域选一）；人名保留准确率约 95%，非 100% 可靠。

**注意事项**：翻译方向为「源语言 → 目标语言」，源语言默认跟随转写语言设置；切换术语偏好后仅对新翻译生效；部分模糊名称可能被命名实体识别误识别，建议重要场景人工校对。

**异常处理**：命名实体识别失败时按通用模型翻译，不阻断流程；语言列表加载失败时显示重试按钮。

**原型图**：

> ![AI翻译](./screenshots/09_ai_translate.png)

**用户流程**：

```
AI翻译
    │
    ├─ 默认翻译语言 ──→ 翻译语言页 → 选择语言 → 返回同步
    │
    ├─ 专业术语偏好 ──→ 专业术语偏好页 → 选择领域 → 返回同步
    │
    └─ 人名/专名保留 开关→即时生效
```

#### 2.6.1 默认翻译语言

**功能描述**：点击进入默认翻译语言页面，展示 7 种语言选择列表。

**功能场景使用**：用户从 7 种语言中选择目标翻译语言，选择后即时生效。

**字段表格**：

| 字段   | 类型 | 可选值                                           |
| ---- | -- | --------------------------------------------- |
| 翻译语言 | 枚举 | 中文（简体）、English、日本語、한국어、Français、Deutsch 等 7 种 |

**交互情况**：进入语言选择页 → 选择语言 → 即时生效并返回。

**边缘情况**：翻译语言与转写语言独立配置。翻译方向为「源语言 → 目标语言」，源语言默认跟随转写语言设置。

**限制条件**：支持 7 种语言可选；单选项。

**注意事项**：源语言默认跟随转写语言设置；翻译方向为「源语言 → 目标语言」。

**异常处理**：语言列表加载失败时显示重试按钮。

**原型图**：同上 AI翻译原型图。

**用户流程**：点击默认翻译语言入口 → 选择语言 → 返回同步。

#### 2.6.2 专业术语偏好

**功能描述**：点击进入专业术语偏好页面，6 个领域可选（通用/法律/金融/医疗/科技/学术）。

**功能场景使用**：医生选择"医疗"领域后，专业术语翻译优先使用医疗领域词典。

**字段表格**：

| 字段   | 类型 | 可选值               |
| ---- | -- | ----------------- |
| 术语偏好 | 枚举 | 通用/法律/金融/医疗/科技/学术 |

**交互情况**：进入专业术语偏好页 → 从 6 个领域中选择 → 确认后即时生效并返回。

**边缘情况**：单选项，切换到「通用」则按通用模型翻译。

**限制条件**：6 个领域选一，单选项。

**注意事项**：选择特定领域后该领域专业术语翻译优先使用领域词典；切换「通用」按通用模型翻译。

**异常处理**：领域列表加载失败时显示重试按钮。

**原型图**：同上 AI翻译原型图。

**用户流程**：点击专业术语偏好入口 → 选择领域 → 返回同步。

#### 2.5.3 人名/专名保留

**功能描述**：开关，开启后翻译时保留人名、公司名等专有名词不翻译。

**功能场景使用**：市场营销会议中开启，"Apple"不被翻成"苹果"、"SoftBank"不被翻成"软银"。

**字段表格**：

| 字段      | 类型 | 默认值   |
| ------- | -- | ----- |
| 人名/专名保留 | 布尔 | false |

**交互情况**：进入 AI翻译页，点击开关即时生效。

**边缘情况**：依赖 AI 命名实体识别，准确率约 95%，部分模糊名称可能被误识别。

**限制条件**：命名实体识别非 100% 可靠，约 95% 准确率。

**注意事项**：专名保留适用于市场、营销、品牌相关场景；部分模糊名称可能被误识别，建议重要场景人工校对。

**异常处理**：命名实体识别失败时按通用翻译处理，不阻断流程。

**原型图**：同上 AI翻译原型图。

**用户流程**：点击开关→即时生效。

***

### 2.6 AI声音

**功能描述**：管理 AI 语音播报的声音方案。支持选择系统预设声音（默认男声 / 默认女声）、创建自定义声音（通过上传音频生成个性化音色）、调节全局语速（0.5x - 2.0x），并提供创意灵感推荐。

**功能场景使用**：

- 小赵买耳机后直接选"默认女声"——觉得女声朗读更亲切。
- 小明想搞搞笑方言口音，点击「+ 创建声音」，起名"搞笑方言"，上传东北话音频生成专属音色。
- 小红看到创意灵感区"🤖 搞怪机器人"，点击后自动预填，只上传音频就完成创建。
- 陈叔叔将语速滑块拖到 0.8x，每次 AI 朗读纪要都用这个速度。

**字段表格**：

| 字段      | 类型     | 说明                             |
| ------- | ------ | ------------------------------ |
| 当前声音    | 枚举/自定义 | 默认男声/默认女声/自定义声音                |
| 语速      | 浮点数    | 0.5x - 2.0x，默认 1.00x           |
| 自定义声音名称 | 字符串    | 最长 20 字符                       |
| 自定义声音描述 | 字符串    | 最长 30 字符                       |
| 自定义声音图标 | 表情符号   | 10 个备选                         |
| 音频样本    | 文件     | MP3/WAV/M4A，建议 10-30 秒，上限 10MB |

**交互情况**：点击系统声音项选中高亮 + 轻提示；拖动语速滑块实时调节；顶部预览区试听播放/暂停（5 秒自动停止）；点击「+ 创建声音」或灵感卡片弹出半屏创建弹窗。

**边缘情况**：声音为全局设置，影响所有播报场景；语速为全局参数不随声音切换变化；自定义声音生成需 1-2 分钟；删除自定义声音后自动回退默认男声；版权责任归属上传者。

**限制条件**：系统声音仅 2 个（默认男声/默认女声）；自定义声音名称最长 20 字符、描述最长 30 字符；音频样本支持 MP3/WAV/M4A，上限 10MB，建议 10-30 秒；语速范围 0.5x-2.0x。

**注意事项**：声音选择影响所有语音播报场景（录音回放、翻译播报、纪要朗读等），为全局设置；语速为全局参数，切换声音后保持不变；试听 5 秒自动停止避免消耗流量；本平台仅提供声音生成技术，用户上传内容不得侵犯第三方权益，法律责任由上传者承担。

**异常处理**：自定义声音生成失败时轻提示"生成失败，请重试"并保留已填信息；音频格式不支持时轻提示"请上传 MP3/WAV/M4A 格式文件"；文件超过 10MB 时阻止上传并轻提示；删除自定义声音后如为当前使用自动回退默认男声。

**原型图**：

> ![AI声音](./screenshots/10_ai_voice.png)

**用户流程**：

```
AI声音
    │
    ├─ 点击系统声音项 → 选中高亮 → 顶部预览区同步更新+轻提示
    │
    ├─ 拖动语速滑块 → 实时更新语速值
    │
    ├─ 点击试听按钮 → 播放/暂停（5秒自动停止）
    │
    ├─ 点击「+ 创建声音」→ 创建弹窗
    │   ├─ 输入名称
    │   ├─ 输入风格描述
    │   ├─ 选择图标（表情符号）
    │   ├─ 上传音频样本
    │   ├─ 阅读免责声明
    │   └─ 点击「开始生成自定义声音」→ 轻提示 → 添加到列表 → 弹窗关闭
    │
    ├─ 点击创意灵感卡片 → 创建弹窗（预填名称+描述+图标）
    │
    ├─ 点击自定义声音项 → 选中该声音为当前声音
    │
    └─ 点击自定义声音删除按钮 → 删除 → 如为当前声音则回退默认男声
```

#### 2.7.1 功能目标

**功能描述**：让用户自由选择和管理朗读播报、翻译播报等场景中使用的声音风格。提供两个系统默认声音（男声/女声），同时开放自定义声音创建能力。

**功能场景使用**：用户进入 AI声音页，选择系统声音直接点击切换，创建自定义声音点击「+ 创建声音」或灵感卡片。

**字段表格**：同 2.7 AI声音字段表格。

**交互情况**：系统声音列表中点击切换默认男声/女声；自定义声音列表展示已创建的声音，空状态引导创建；创意灵感卡片点击跳转创建弹窗并自动预填。

**边缘情况**：声音选择影响所有语音播报场景，为全局设置。自定义声音生成需 1-2 分钟。

**限制条件**：系统声音仅 2 个；自定义声音需上传音频生成。

**注意事项**：声音选择为全局设置；自定义声音生成需 1-2 分钟请耐心等待。

**异常处理**：声音列表加载失败时显示重试按钮。

**原型图**：同上 AI声音原型图。

**用户流程**：同 2.7 AI声音用户流程。

##### 设计理由

- **为什么只有两个默认声音？** 旧版 4 种具名音色命名带主观色彩，音色越多越难选。简化为「默认男声」「默认女声」两个中性选项，降低选择焦虑。
- **为什么用"自定义声音"替代"声音克隆"？** "克隆"有法律风险——暗示可精准复制他人声音。改用"自定义声音"降低法律风险敞口，同时嵌入免责声明。
- **为什么提供创意灵感？** 用户不会主动想"创建什么声音"，但看到有趣角色时会产生冲动。横向滚动的灵感卡片降低创作门槛。

#### 2.7.2 页面结构

**功能描述**：展示 AI声音页面的完整结构布局——当前已选声音预览区（头像图标+名称+描述+语速滑块+试听按钮）、系统声音列表、自定义声音列表、创意灵感横向滚动卡片区。

**功能场景使用**：顶部预览区实时展示当前选中声音，支持试听播放/暂停。

**字段表格**：同 2.7 AI声音字段表格。

**交互情况**：预览区实时展示+试听播放/暂停（5 秒自动停止）；系统声音列表点击切换；自定义声音列表空状态展示引导；创意灵感卡片点击跳转创建弹窗。

**边缘情况**：试听 5 秒自动停止避免消耗过多流量；空状态引导文案：「还没有自定义声音」+「开始创建」按钮。

**限制条件**：试听按钮 5 秒自动停止。

**注意事项**：空状态引导用户创建自定义声音；空状态文案为固定文案。

**异常处理**：页面结构加载失败时显示重试按钮。

**原型图**：同上 AI声音原型图。

**用户流程**：同 2.7 AI声音用户流程。

#### 2.7.3 创建自定义声音弹窗

**功能描述**：点击「+ 创建声音」或灵感卡片后弹出半屏弹窗，包含声音名称（最长 20 字符）、风格描述（最长 30 字符）、图标选择（10 个表情符号）、音频样本上传（MP3/WAV/M4A，建议 10-30 秒，上限 10MB）及版权免责声明。名称+音频均填写后确认按钮激活。

**功能场景使用**：用户点击「+ 创建声音」→ 输入名称和描述 → 选择表情符号图标 → 上传音频 → 确认生成。灵感卡片自动预填名称和描述。

**字段表格**：

| 字段   | 类型   | 规则                                |
| ---- | ---- | --------------------------------- |
| 声音名称 | 字符串  | 最长 20 字符，必填                       |
| 风格描述 | 字符串  | 最长 30 字符，选填                       |
| 图标   | 表情符号 | 10 个备选，单选                         |
| 音频样本 | 文件   | MP3/WAV/M4A，建议 10-30 秒，上限 10MB，必填 |

**交互情况**：点击「+ 创建声音」或灵感卡片 → 半屏弹窗 → 填写信息 → 上传音频 → 确认生成。灵感卡片自动预填名称和描述。

**边缘情况**：音频超过 30 秒自动截取前 30 秒；文件上限 10MB；生成声音可随时删除，删除后自动回退默认男声；免责声明明确版权责任归属上传者。

**限制条件**：音频样本支持 MP3/WAV/M4A，上限 10MB，建议 10-30 秒；名称最长 20 字符；描述最长 30 字符。

**注意事项**：名称+音频均填写后确认按钮才激活；灵感卡片自动预填名称和描述。

**异常处理**：音频超过 30 秒自动截取前 30 秒；格式不支持时轻提示"请上传 MP3/WAV/M4A 格式文件"；文件超过 10MB 阻止上传并轻提示。

**原型图**：同上 AI声音原型图。

**用户流程**：创建声音弹窗 → 填写+上传 → 确认生成 → 轻提示 → 添加到列表。

#### 2.6.4 语速调节

**功能描述**：语速滑块范围 0.5x - 2.0x，默认 1.00x。拖动实时显示当前值，语速为全局参数。

**功能场景使用**：用户拖动滑块调节语速，预览区试听按钮使用当前语速播放。

**字段表格**：

| 字段 | 类型  | 范围          | 默认值   |
| -- | --- | ----------- | ----- |
| 语速 | 浮点数 | 0.5x - 2.0x | 1.00x |

**交互情况**：拖动滑块调节，松开即时生效。预览区试听使用当前语速播放。

**边缘情况**：语速为全局参数，切换声音后保持不变。0.5x 适合语言学习，2.0x 适合快速回听。

**限制条件**：语速范围 0.5x-2.0x，全局参数。

**注意事项**：0.5x 适合语言学习场景；2.0x 适合快速回听场景。

**异常处理**：语速设置保存失败时轻提示"保存失败，请重试"。

**原型图**：同上 AI声音原型图。

**用户流程**：拖动滑块 → 实时更新 → 松手生效。

#### 2.7.5 版权与合规

**功能描述**：自定义声音不预设任何名人/角色音色；创建弹窗底部显式展示免责声明；每个自定义声音均支持删除。

**功能场景使用**：用户创建声音时弹窗底部自动展示免责声明文案，删除按钮在自定义声音列表每项右侧。

**字段表格**：无独立数据字段。

**交互情况**：创建弹窗底部自动展示免责声明；删除按钮在自定义声音列表每项右侧。

**边缘情况**：本平台仅提供声音生成技术，不提供预设名人/角色音色。用户上传内容不得侵犯第三方权益，法律责任由上传者承担。

**限制条件**：平台仅提供声音生成技术，不提供预设音色。

**注意事项**：用户上传内容不得侵犯第三方权益（含肖像权、著作权、声音权）；法律责任由上传者承担。

**异常处理**：违规内容由后台审核拦截，轻提示"内容不符合平台规范"。

**原型图**：同上 AI声音原型图。

**用户流程**：无需额外操作，免责声明在创建弹窗底部自动展示。

***

### 2.7 记忆管理

**功能描述**：控制 AI 记忆库开关。开启后 AI 会记住用户主动要求记住的信息并自动从对话中提取内容，在后续对话中提供更个性化的体验。关闭不删除已有记忆，再次开启后记忆数据保留。

**功能场景使用**：周总监在周会中说"记住：项目截止日是 8 月 15 日"，后续会议中 AI 自动引用该信息提醒团队。发现记忆过时可关闭后重新开启，数据保留。

**字段表格**：

| 字段    | 类型 | 默认值   | 说明 |
| ----- | -- | ----- | -- |
| AI记忆库 | 布尔 | false | 开关 |

**交互情况**：进入记忆管理页，点击开关即时生效，首页状态同步更新。点击「了解更多」弹出详情弹窗。

**边缘情况**：关闭不删除已有记忆，再次开启之前记忆数据保留不动。

**限制条件**：AI 记忆库上限取决于用量套餐档位；仅记忆用户主动要求记住的信息和 AI 自动提取的关键内容。

**注意事项**：关闭开关不会删除已有记忆数据，再次开启后之前的记忆继续可用；记忆内容仅本账号可见，切换账号不共享记忆数据。

**异常处理**：记忆库读写失败时轻提示"记忆服务暂不可用"，不影响录音和转写功能。

**原型图**：

> ![记忆管理](./screenshots/12_memory.png)

**用户流程**：

```
AI录音
    │
    ├─ 无痕模式 开关 → 即时生效
    │
    ├─ AI生成模式 ──→ AI生成模式页
    │   ├─ 选择「手动生成」
    │   └─ 选择「自动生成」→ 展开配置项
    │       ├─ 触发时长选择 → 选项弹窗
    │       └─ 长度上限选择 → 选项弹窗
    │
    └─ 默认模板总结 ──→ 默认模板总结页
        ├─ 点击模板卡片 → 选中高亮 + 详情弹窗
        └─ 自定义模板（旗舰版专属）
```

***

### 2.8 通用设置

**功能描述**：APP 的全局配置中心。涵盖外观与显示（界面语言 / 主题模式 / 字体大小）、通知与存储（推送通知 / 清除缓存）、个性功能（个人词库 / 声纹管理）三大类设置，是用户定制 APP 使用体验的集中入口。

**功能场景使用**：

- 王桑将界面语言切换到日语，菜单和提示全变成日文。
- 小陈将主题切到"深色"减少夜间刺眼。
- 刘老师将字体调到"大"看清会议记录。
- 赵总将"量化宽松"等术语录入个人词库提升转写准确率。
- 张总和李经理分别录声纹，APP 自动区分说话人。
- 设备存储报警时用户清除缓存释放空间。

**字段表格**：

| 设置分类  | 设置项  | 说明         |
| ----- | ---- | ---------- |
| 外观与显示 | 界面语言 | 4 种语言      |
| 外观与显示 | 主题模式 | 跟随系统/浅色/深色 |
| 外观与显示 | 字体大小 | 5 级滑块      |
| 通知与存储 | 允许通知 | 开关         |
| 通知与存储 | 清除缓存 | 显示当前大小     |
| 个性功能  | 个人词库 | 录音词库+翻译词库  |
| 个性功能  | 声纹管理 | 声纹录入+列表    |

**交互情况**：点击对应设置项进入子页面配置。界面语言和主题模式即时切换，字体大小滑块调节实时预览，允许通知开关即时生效，清除缓存需二次确认。

**边缘情况**：界面语言切换需重新加载部分页面；清除缓存不删除录音和转写数据；词库最多 200 条录音热词和 100 条翻译映射对；声纹最多 5 个，达到上限隐藏新增按钮。

**限制条件**：界面语言支持 4 种；主题模式 3 种；字体大小 5 级；个人词库最多 200 条录音热词和 100 条翻译映射对；声纹最多 5 个；缓存清除不可恢复。

**注意事项**：界面语言切换后部分页面需重新加载；字体最大「大」级比默认「标准」大约 150%；清除缓存仅删除临时文件，不删除录音/转写文本/个人数据；声纹达到上限时隐藏新增按钮。

**异常处理**：语言切换后页面渲染异常时回退到默认语言并轻提示；缓存清除失败时轻提示"清除失败，请稍后重试"；词库为空时显示空状态占位文案。

**原型图**：

> ![通用设置](./screenshots/13_general_settings.png)

**用户流程**：

```
通用设置
    │
    ├─ 外观与显示
    │   ├─ 界面语言 ──→ 界面语言页 → 选择语言 → 返回
    │   ├─ 主题模式 ──→ 主题模式页 → 选择主题 → 返回
    │   └─ 字体大小 ──→ 字体大小页 → 拖动滑块 → 实时预览
    │
    ├─ 通知与存储
    │   ├─ 允许通知开关
    │   └─ 清除缓存 → 二次确认 → 轻提示
    │
    └─ 个性功能
        ├─ 个人词库 ──→ 个人词库页
        │   ├─ 录音词库标签页：添加热词/批量管理/多选删除
        │   └─ 翻译词库标签页：添加映射对/批量管理/多选删除
        │
        └─ 声纹管理 ──→ 声纹管理页
            ├─ 列表展示已录声纹
            ├─ 新增声纹 → 权限请求 → 命名 → 朗读录制
            └─ 删除声纹 → 二次确认
```

#### 2.8.1 外观与显示

**功能描述**：外观与显示设置区，包含界面语言（简体中文/English/日本語/한국어）、主题模式（跟随系统/浅色模式/深色模式）、字体大小（5 级滑块：小/较小/标准/较大/大，带预览区域）。

**功能场景使用**：用户点击进入对应子页面配置外观偏好。

**字段表格**：

| 设置项  | 类型 | 可选值                  |
| ---- | -- | -------------------- |
| 界面语言 | 枚举 | 简体中文/English/日本語/한국어 |
| 主题模式 | 枚举 | 跟随系统/浅色模式/深色模式       |
| 字体大小 | 枚举 | 小/较小/标准/较大/大         |

**交互情况**：界面语言和主题模式为即时切换，字体大小通过滑块调节并实时预览。

**边缘情况**：界面语言切换影响整个 APP 菜单和提示文本，需重新加载部分页面。字体大小最大「大」级比默认「标准」大约 150%。

**限制条件**：界面语言 4 种可选；主题模式 3 种；字体大小 5 级。

**注意事项**：界面语言切换需重新加载部分页面；字体大小最大「大」级约 150%。

**异常处理**：语言切换后页面渲染异常回退默认语言并轻提示。

**原型图**：

> ![界面语言](./screenshots/13a_lang.png)
> ![主题模式](./screenshots/13b_theme.png)
> ![字体大小](./screenshots/13c_font_size.png)

**用户流程**：点击子设置项 → 进入配置页 → 选择/调节 → 返回。

#### 2.8.2 通知与存储

**功能描述**：通知与存储管理，包含允许通知的开关和清除缓存功能（显示当前缓存大小如 86 MB，点击清除）。

**功能场景使用**：用户关闭推送通知避免打扰；设备存储报警时点击清除缓存释放空间。

**字段表格**：

| 设置项  | 类型  | 说明      |
| ---- | --- | ------- |
| 允许通知 | 布尔  | 开关即时生效  |
| 缓存大小 | 字符串 | 如 86 MB |

**交互情况**：允许通知为开关即时生效；点击清除缓存触发二次确认弹窗，确认后清除并更新显示缓存大小。

**边缘情况**：清除缓存仅删除本地临时文件，不会删除录音文件、转写文本和个人数据。

**限制条件**：仅删除本地临时文件（音频缓存、图片缓存等）。

**注意事项**：清除后缓存从 0 开始累计；不会删除录音文件、转写文本和个人数据。

**异常处理**：清除缓存失败时轻提示"清除失败，请稍后重试"。

**原型图**：同上通用设置原型图。

**用户流程**：开关 / 点击清除缓存 → 二次确认 → 轻提示。

#### 2.8.3 个性功能

**功能描述**：个性功能入口区，包含个人词库（跳转个人词库页，双 标签页：录音词库/翻译词库）和声纹管理（跳转声纹管理页，显示已录声纹人数）。

**功能场景使用**：用户点击个人词库进入管理热词，点击声纹管理录入声纹。

**字段表格**：

| 入口   | 显示信息   | 目标页面  |
| ---- | ------ | ----- |
| 个人词库 | 词库数量   | 个人词库页 |
| 声纹管理 | 已录声纹人数 | 声纹管理页 |

**交互情况**：点击个人词库进入词库管理——录音词库标签页 添加热词，翻译词库标签页 添加映射对；点击声纹管理进入声纹录入和管理。

**边缘情况**：个人词库最多 200 条录音热词和 100 条翻译映射对；声纹最多 5 个，达到上限隐藏新增按钮；词库为空时显示空状态占位文案。

**限制条件**：个人词库最多 200 条录音热词 + 100 条翻译映射对；声纹最多 5 个。

**注意事项**：达到上限时隐藏新增按钮；词库为空时显示空状态占位文案。

**异常处理**：词库数据加载失败时显示重试按钮；声纹录入失败时轻提示"录入失败，请重试"。

**原型图**：

> ![个人词库](./screenshots/13d_hot_words.png)
> ![声纹管理](./screenshots/13e_voiceprint.png)

**用户流程**：点击个人词库/声纹管理入口 → 进入对应管理页。

***

### 2.9 专属客服

**功能描述**：提供 FAQ 搜索、分类浏览和人工客服联系渠道，帮助用户自助解决常见问题或联系一对一人工客服。覆盖设备连接、录音转写、翻译功能、账号会员、其他问题五大类 FAQ，支持关键词检索和展开/收起交互，同时提供企业微信客服二维码供用户扫码添加专属客服。

**功能场景使用**：

- 新用户小刘连接不上蓝牙，搜索框输入"蓝牙"找到 FAQ 按步骤排查后成功连上。
- 赵总在使用自定义声音时遇到无法上传文件的问题，FAQ 未找到答案，扫描企微二维码联系人工客服 5 分钟内解决。

**字段表格**：

| 元素      | 说明                              |
| ------- | ------------------------------- |
| FAQ 分类  | 设备连接问题/录音转写功能/翻译功能使用/账号与会员/其他问题 |
| 搜索栏     | 输入关键词检索 FAQ                     |
| 企微客服二维码 | 扫码添加企微客服                        |

**交互情况**：搜索栏输入关键词动态筛选 FAQ；点击分类卡片展开 FAQ 列表；扫描企微二维码外部打开企微添加客服；点击 FAQ 分类快捷入口展开列表。

**边缘情况**：搜索结果为空时显示"未找到相关问题"提示；二维码失效时需定期更新。

**限制条件**：FAQ 内容为静态数据，需版本更新时刷新；仅支持文字搜索；企微客服二维码需后台定期更新确保有效。

**注意事项**：FAQ 内容可能不覆盖所有边缘场景；扫码后将在外部企微 APP 中打开；客服响应时间为工作日 9:00-18:00，非实时在线。

**异常处理**：搜索无结果时显示"未找到相关问题，请尝试其他关键词"；FAQ 内容加载失败时显示重试按钮；二维码加载失败时显示占位图并提示；外部跳转失败时轻提示"无法打开，请确认已安装对应应用"。

**原型图**：

> ![帮助与反馈](./screenshots/14_help_feedback.png)
> ![专属客服](./screenshots/15_customer_service.png)

**用户流程**：

```
专属客服
    │
    ├─ 搜索栏输入关键词 → 动态筛选FAQ
    │
    ├─ 点击分类卡片 → 展开FAQ列表
    │   └─ 点击具体FAQ → 展开/收起答案
    │
    ├─ 扫描企微二维码 → 外部跳转
    │
    └─ 点击 FAQ 分类 → 展开FAQ列表
```

#### 2.9.1 帮助与反馈

**功能描述**：提供 FAQ 搜索和分类浏览功能，帮助用户自助解决常见问题。覆盖设备连接、录音转写、翻译功能、账号会员、其他问题五大类，支持关键词检索和展开/收起交互。

**功能场景使用**：新用户小刘连接不上蓝牙，搜索框输入"蓝牙"找到 FAQ 按步骤排查后成功连上。

**字段表格**：

| FAQ 分类 | 覆盖内容            |
| ------ | --------------- |
| 设备连接问题 | 蓝牙配对、断连、多设备切换   |
| 录音转写功能 | 识别准确率、语言支持、导出格式 |
| 翻译功能使用 | 实时翻译、语种选择       |
| 账号与会员  | 注册登录、套餐升级、支付问题  |
| 其他问题   | 固件更新、售后服务、隐私政策  |

**交互情况**：搜索栏输入关键词动态筛选 FAQ；点击分类卡片展开 FAQ 列表；点击具体 FAQ 展开/收起答案。

**边缘情况**：搜索结果为空时显示"未找到相关问题"提示。

**限制条件**：FAQ 内容为静态数据，需版本更新时刷新；仅支持文字搜索，不支持语音和图片搜索。

**注意事项**：FAQ 内容可能不覆盖所有边缘场景，复杂问题建议通过人工客服咨询；部分 FAQ 答案需要联网获取。

**异常处理**：搜索无结果时显示"未找到相关问题，请尝试其他关键词"；FAQ 内容加载失败时显示重试按钮。

**原型图**：

> ![帮助与反馈](./screenshots/14_help_feedback.png)

**用户流程**：

```
帮助与反馈
    │
    ├─ 搜索栏输入关键词 → 动态筛选FAQ
    │
    └─ 点击分类卡片 → 展开FAQ列表
        └─ 点击具体FAQ → 展开/收起答案
```

#### 2.9.2 人工客服

**功能描述**：提供人工客服联系渠道。用户可通过企微客服二维码扫码添加专属客服，获得一对一问题解答。

**功能场景使用**：赵总在使用自定义声音时遇到无法上传文件的问题，FAQ 未找到答案，打开专属客服页扫描企微二维码联系人工客服解决。

**字段表格**：

| 元素      | 说明       |
| ------- | -------- |
| 企微客服二维码 | 扫码添加企微客服 |

**交互情况**：扫描企微二维码 → 外部打开企微添加客服。

**边缘情况**：二维码失效时需定期更新。

**限制条件**：企微客服二维码为静态图片，需后台定期更新确保有效。

**注意事项**：扫码后将在外部浏览器/企微 APP 中打开，非 APP 内功能；客服响应时间为工作日 9:00-18:00，非实时在线。

**异常处理**：二维码加载失败时显示占位图并提示"二维码加载失败，请稍后重试"；外部跳转失败时轻提示"无法打开，请确认已安装对应应用"。

**原型图**：

> ![专属客服](./screenshots/15_customer_service.png)

**用户流程**：

```
人工客服
    │
    ├─ 扫描企微二维码 → 外部跳转
    │
    └─ 点击 FAQ 分类 → 展开FAQ列表
```

***

### 2.10 关于

**功能描述**：展示 APP 基本信息与法律合规入口。包含版本号（支持检查更新）、公司官网、官方媒体入口（公众号 / 抖音 / 小红书 / 视频号）、法律文档（隐私政策 / 用户协议 / 第三方信息共享清单等）以及版权信息。

**功能场景使用**：

- 小张点击版本号检查更新下载新版本。
- 新用户注册时点击法律文档查看隐私政策详情。
- 小刘点击公众号/抖音图标跳转关注了解最新活动。

**字段表格**：

| 元素   | 说明                                  |
| ---- | ----------------------------------- |
| 版本号  | 当前版本（如 v5.4.0），有新版本红色角标             |
| 公司官网 | 确认弹窗后打开外部链接                         |
| 官媒入口 | 公众号/抖音/小红书/视频号，圆形图标                 |
| 法律文档 | 隐私政策概要、个人信息清单、第三方信息共享清单、用户服务协议、隐私政策 |
| 版权信息 | 版权声明 + 备案号                          |

**交互情况**：点击版本号触发检查更新 → 轻提示/模拟更新流程；点击公司官网确认弹窗后外部跳转；点击官媒图标外部跳转；点击法律文档弹窗展示内容。

**边缘情况**：无更新时轻提示"当前已是最新版本"。

**限制条件**：版本号不可手动修改，由打包构建时自动写入；官媒入口和法律文档链接为静态链接，需版本更新时刷新。

**注意事项**：点击公司官网和官媒入口将跳转外部浏览器，非 APP 内功能；法律文档弹窗内容较长，建议在稳定网络环境下查看；版权信息包含备案号，需符合监管部门要求。

**异常处理**：检查更新失败时轻提示"网络异常，请稍后重试"并显示当前版本为最新；外部链接跳转失败时轻提示"无法打开链接"；法律文档加载失败时弹窗内显示重试按钮。

**原型图**：

> ![关于](./screenshots/16_about.png)

**用户流程**：

```
关于
    │
    ├─ 点击版本号 → 检测更新 → 轻提示/模拟更新流程
    │
    ├─ 点击公司官网 → 确认弹窗 → 外部跳转
    │
    ├─ 官媒入口（点击图标 → 外部跳转）
    │
    └─ 法律文档（点击 → 弹窗展示内容）
```

***

## 三、全局交互规范

### 3.1 导航模式

- 所有二级/三级页面顶部均有弹性置顶导航栏，包含返回按钮 + 页面标题
- 返回按钮统一使用左箭头图标
- 页面切换使用单页应用模式，通过导航函数管理页面栈

### 3.2 弹窗组件

| 弹窗类型   | 触发方式   | 关闭方式       |
| ------ | ------ | ---------- |
| 底部操作菜单 | 底部滑出   | 点击遮罩/取消按钮  |
| 选项选择器  | 底部滑出   | 点击遮罩/关闭按钮  |
| 确认弹窗   | 居中显示   | 点击取消/确认/遮罩 |
| 权限弹窗   | 居中显示   | 允许/拒绝      |
| 头像预览   | 全屏黑色背景 | 点击遮罩/ESC键  |
| 轻提示    | 底部短暂显示 | 自动消失       |

### 3.3 轻提示反馈

所有操作（保存、删除、复制、更新）均给出轻提示文字反馈，统一在页面中下部短暂显示后自动消失。

### 3.4 状态同步

- 昵称修改 → 同步首页身份区 + 个人信息页
- 音色/语速修改 → 保存后同步首页 AI声音摘要
- 转写语言修改 → 同步首页 AI录音摘要
- 翻译语言修改 → 同步首页 AI翻译摘要
- 记忆管理开关 → 同步首页状态
- 声纹录入/删除 → 同步通用设置声纹计数
- 头像修改 → 同步首页头像 + 个人信息页头像

